<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fondo1">

    <!-- Barra Superior -->
    <RelativeLayout
        android:id="@+id/topBarCarrito"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/azul_primario"
        android:paddingHorizontal="16dp"
        android:elevation="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btnCarritoAtras"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_centerVertical="true"
            android:src="@android:drawable/ic_menu_revert"
            app:tint="@color/white"
            android:contentDescription="Atrás" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="Tus Compras"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <!-- Header Icon Section -->
    <LinearLayout
        android:id="@+id/headerCarrito"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        app:layout_constraintTop_toBottomOf="@id/topBarCarrito">

        <androidx.cardview.widget.CardView
            android:layout_width="80dp"
            android:layout_height="80dp"
            app:cardCornerRadius="40dp"
            app:cardElevation="6dp">
            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="15dp"
                android:src="@drawable/ic_carrito_compras"
                app:tint="@color/azul_vibrante"
                android:contentDescription="Icono Carrito" />
        </androidx.cardview.widget.CardView>
    </LinearLayout>

    <!-- Content Section -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/headerCarrito"
        app:layout_constraintBottom_toTopOf="@id/footerCarrito">

        <LinearLayout
            android:id="@+id/containerItemsCarrito"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="16dp" />
    </androidx.core.widget.NestedScrollView>

    <!-- Footer Section -->
    <androidx.cardview.widget.CardView
        android:id="@+id/footerCarrito"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardElevation="16dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:background="@color/white">

            <Button
                android:id="@+id/btnComprarTodo"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="PAGAR CON TARJETA"
                android:backgroundTint="@color/azul_vibrante"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginBottom="8dp"/>

            <Button
                android:id="@+id/btnReservarEfectivo"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="RESERVAR (PAGO EN EFECTIVO)"
                android:backgroundTint="@color/azul_suave"
                android:textColor="@color/azul_primario"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginBottom="8dp"/>

            <Button
                android:id="@+id/btnVerHistorialDesdeCarrito"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:text="VER MIS PEDIDOS ANTERIORES"
                android:backgroundTint="@color/white"
                android:textColor="@color/azul_vibrante"
                android:textSize="13sp"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>
