<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.ConexionFarmaco">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RecomendacionesSaludActivity" />
        <activity android:name=".FarmaciasActivity" />
        <activity android:name=".PerfilActivity" />

        <!-- Actividades de Administración -->
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".AdminRegistroActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity
            android:name=".AdminMedicamentoActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".AdminPerfilActivity" />
        <activity android:name=".AdminFacturacionActivity" />
        <activity android:name=".HistorialPedidosActivity" />
        <activity android:name=".FarmaciaDetalleActivity" />
        <activity android:name=".CarritoActivity" />
        <activity android:name=".PagoActivity" />
        <activity
            android:name=".FacturacionActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ResumenPedidoActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="pago-finalizado.com" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChatIAActivity"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity android:name=".ChatMensajeriaActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ListaChatClientesActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.conexionfarmaco.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>